/**
 * @type {import('react').CSSProperties}
 */
export const container = {
  backgroundColor: "#1E1E2A",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "100vh",
  color: "#fff",
};

/**
 * @type {import('react').CSSProperties}
 */
export const buttonStyles = {
  button: {
    backgroundColor: "#F83738", // Red button
    padding: "15px 20px", // Combined padding for web
    borderRadius: "5px",
    marginTop: "10px",
    border: "none",
    color: "#fff",
    fontSize: "16px",
    fontWeight: 600,
    cursor: "pointer", // Ensure pointer cursor for buttons
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
  },
  buttonText: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600",
  },
};
